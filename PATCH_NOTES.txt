patch_0.031 (UI-only, safe)
- 강제 10열 제거, 칩 자동 크기(clamp) + 줄바꿈으로 오버플로우 방지
- 번호색 규칙(1–10 노랑/11–20 파랑/21–30 빨강/31–40 회색/41–45 초록) 유지
- 버튼 행 좌우 동일폭 (.button-row .btn) 기본 스타일 제공
- 추천 리스트를 30세트(=5x6) 블록으로 묶어 뿌리는 window.renderRecoSets() 헬퍼 제공
- index.html 자동 주입 스크립트(scripts_apply_reco_ui.ps1)

미포함(앱 내부 셀렉터/구조 미확정으로 안전훅 부재):
- 제외수 리셋(토글만 해제)과 추천 리셋 분리 로직
- 추천 시 자동 저장(저장번호에 즉시 병합)
- 추천 화면 DOM 재구성(기존 렌더러 교체)

적용 방법 (PowerShell):
pwsh -File .\scripts_apply_reco_ui.ps1
git add index.html styles.reco-ui.css js/reco-ui.js
git commit -m "patch_0.031: responsive chips + reco blocks helper"
git push

호스트앱에서 window.renderRecoSets(sets, optionalMount) 호출 시 30세트 블록 UI로 표시됨.
