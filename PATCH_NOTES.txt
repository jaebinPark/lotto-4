patch_0.041
- Add window.$store (versioned, TTL, safe localStorage wrapper)
- Add window.$safe + window.$bus (global safety helpers and event bus)
- Add window.$render (render bridge; ensure #app exists, safe mount())
- Provide Windows PowerShell script 'scripts_apply_core_winps.ps1' that injects <script> tags into index.html BEFORE app.bundle.js
- No behavior changes to app bundle; helpers are passive until used.
